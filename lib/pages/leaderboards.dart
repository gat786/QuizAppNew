import 'package:flutter/material.dart';
import 'package:quiz/ui/leaderbox.dart';

class LeaderBoard extends StatelessWidget{
  @override
    Widget build(BuildContext context) {
      // TODO: implement build
      return new Material(
        child: new LeaderBox(),
      );
    }
}