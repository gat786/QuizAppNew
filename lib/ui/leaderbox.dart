import 'package:flutter/material.dart';

class LeaderBox extends StatelessWidget{
  //final List<dynamic> names,scores; 
  //LeaderBox(this.names,this.scores);

  @override
    Widget build(BuildContext context) {
      // TODO: implement build
      return new Material(
      );
    }
}